defmodule Math do
  def two_become_one(:add , x, y), do: x + y
  def two_become_one(:subtract, x, y), do: x - y
  def two_become_one(:divide, x, y), do: x / y
  def two_become_one(:multiply, x, y), do: x * y
end    
